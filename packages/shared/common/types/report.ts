export enum ReportType {
  BROKEN = 'BROKEN',
  MISSING = 'MISSING',
}
export enum ReportStatus {
  WAITING = 'WAITING',
  ACCEPTED = 'ACCEPTED',
}

export enum EReportType {
  NONE = 'NONE',
  BROKEN = 'BROKEN',
  MISSING = 'MISSING',
}
export enum EReportStatus {
  NONE = 'NONE',
  WAITING = 'WAITING',
  ACCEPTED = 'ACCEPTED',
}
